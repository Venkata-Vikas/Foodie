<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Foodies.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand">
            <StackLayout VerticalOptions="CenterAndExpand" Padding="20">
                <Label Text="Welcome to Foodies App" FontSize="35" HorizontalOptions="CenterAndExpand" Margin="0,0,0,100" FontAttributes="Bold" TextColor="Blue"></Label>
                <StackLayout Margin="20,0,20,10">
                <Entry Placeholder="Username" Text="{Binding Username}" Margin="0,0,0,20" FontSize="Medium"
                               HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand"/>
                <Entry Placeholder="Password"  Text="{Binding Password}" FontSize="Medium"
                               IsPassword="True" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand"/>
                </StackLayout>
                <Button x:Name="login" Text="LOGIN" Command="{Binding LogInCommand}" BackgroundColor="MediumBlue" Padding="50,10" TextColor="White" Margin="0,30,0,0" HorizontalOptions="Center" CornerRadius="20" FontAttributes="Bold"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

